# LIBRERÍA RANDOM (ya esta integrada en python)
import random

# Utilizamos random.randint para generar un número entero aleatorio en el rango [1, 10]
numero_aleatorio = random.randint(1, 10)
print(numero_aleatorio)